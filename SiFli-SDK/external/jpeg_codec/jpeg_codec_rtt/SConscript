import os
from building import * 

# get current dir path
cwd = GetCurrentDir()

# init src and inc vars
src = []
inc = []

if GetDepend('USING_JPEG_DEC'):
    src += ['jpeg_dec_rtt.c']

# add group to IDE project
objs = DefineGroup('JpegAccCodec2RTT', src, depend = [''], CPPPATH = inc)

Return('objs')